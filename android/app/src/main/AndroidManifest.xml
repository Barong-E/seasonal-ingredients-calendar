<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- ======================================================= -->
        <!--  위젯 등록 (7종)                                          -->
        <!-- ======================================================= -->

        <!-- ① 제철 마이크로 (1×1) -->
        <receiver
            android:name=".widget.SeasonalMicroWidget"
            android:exported="true"
            android:label="제철 마이크로">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/seasonal_micro_info" />
        </receiver>

        <!-- ② 명절 마이크로 (1×1) -->
        <receiver
            android:name=".widget.HolidayMicroWidget"
            android:exported="true"
            android:label="명절 마이크로">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/holiday_micro_info" />
        </receiver>

        <!-- ③ 제철 미니 (2×1) -->
        <receiver
            android:name=".widget.SeasonalMiniWidget"
            android:exported="true"
            android:label="제철 미니">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/seasonal_mini_info" />
        </receiver>

        <!-- ④ 명절 미니 (2×1) -->
        <receiver
            android:name=".widget.HolidayMiniWidget"
            android:exported="true"
            android:label="명절 미니">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/holiday_mini_info" />
        </receiver>

        <!-- ⑤ 하이브리드 스퀘어 (2×2) -->
        <receiver
            android:name=".widget.HybridSquareWidget"
            android:exported="true"
            android:label="하이브리드 스퀘어">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/hybrid_square_info" />
        </receiver>

        <!-- ⑥ 하이브리드 배너 (4×1) -->
        <receiver
            android:name=".widget.HybridBannerWidget"
            android:exported="true"
            android:label="하이브리드 배너">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/hybrid_banner_info" />
        </receiver>

        <!-- ⑦ 하이브리드 대시보드 (4×2) -->
        <receiver
            android:name=".widget.HybridDashboardWidget"
            android:exported="true"
            android:label="하이브리드 대시보드">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/hybrid_dashboard_info" />
        </receiver>

        <!-- Google AdMob App ID (개발 및 테스트용 구글 공식 테스트 ID 선등록) -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713"/>

    </application>

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="true" />
</manifest>

